# jsonschemaprofiles

A Go library for validating that a **JSON Schema itself** conforms to provider-specific structured-output restrictions (for example Gemini and OpenAI).

This project is about schema-profile validation, not JSON instance/payload validation.
